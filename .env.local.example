RESEND_API_KEY=
CONTACT_TO_EMAIL=Eludwig1126@gmail.com

# Local-development override only. Production defaults to https://ludwigroofing.org
# (see lib/site.ts) — leave this unset in the production environment.
# Must be on a domain verified in Resend, or sends will 403.
CONTACT_FROM_EMAIL="Ludwig's Roofing <quotes@ludwigroofing.org>"
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Business Profile "write a review" short link.
# Get it from business.google.com → Ask for reviews → copy link.
# Leave blank to hide the review CTA until the profile is claimed.
NEXT_PUBLIC_GOOGLE_REVIEW_URL=
